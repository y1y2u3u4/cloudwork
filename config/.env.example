# =====================================
# CloudWork Configuration
# =====================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# =====================================
# Telegram Bot (Required)
# =====================================

# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Comma-separated user IDs allowed to use the bot
# Get your ID from @userinfobot on Telegram
TELEGRAM_ALLOWED_USERS=123456789,987654321

# =====================================
# Claude API (Choose one option)
# =====================================

# Option 1: Official Anthropic API
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Option 2: Custom API Proxy (comment out Option 1 if using this)
# ANTHROPIC_BASE_URL=https://your-proxy.com/api
# ANTHROPIC_AUTH_TOKEN=your_auth_token

# =====================================
# Claude Settings (Optional)
# =====================================

# Default model: sonnet (default), opus, haiku
DEFAULT_MODEL=sonnet

# Execution mode: auto (default), plan
# auto = skip all confirmations, plan = generate plan first
DEFAULT_MODE=auto

# Command execution timeout in seconds
COMMAND_TIMEOUT=300

# =====================================
# Storage (Optional)
# =====================================

# Working directory (default: /app or current directory)
WORK_DIR=./

# Data directory for sessions (default: ./data)
DATA_DIR=./data

# Workspace directory for tasks (default: ./workspace)
WORKSPACE_DIR=./workspace

# =====================================
# Session Settings (Optional)
# =====================================

# Minutes of inactivity before auto-archiving a session
AUTO_ARCHIVE_MINUTES=30

# Maximum concurrent task executions
MAX_CONCURRENT_TASKS=5

# =====================================
# Logging (Optional)
# =====================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log file path (default: ./logs/cloudwork.log)
LOG_FILE=./logs/cloudwork.log
