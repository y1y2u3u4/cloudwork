# Tool Display 周期性更新覆盖问题

**提取日期**: 2026-01-25
**来源**: cloudwork tool_display bug fix
**标签**: bug, async, state

## 问题
工具调用时显示详细参数（如 📖 读取 .../file.py），但 1.5 秒后被周期性进度更新覆盖为简单格式（⏳ Read 执行中...）。

## 根因
- tool_use 事件时调用 tool_formatter.format() 生成格式化显示
- 但 task.current_tool 只保存原始工具名
- 周期性更新用 task.current_tool 重新拼接，丢失格式化信息

## 解决方案
1. 在 RunningTask 类添加 current_tool_display 字段
2. tool_use 时同时保存格式化结果到 task.current_tool_display
3. 周期性更新直接使用 task.current_tool_display

## 适用场景
任何需要在异步任务中保持状态一致性的场景。